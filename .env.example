# ---- Ecomap environment ----
# Live AI research is OPTIONAL. Without these, Ecomap runs fully offline using
# its deterministic research-methodology engine + the hand-authored benchmark case.

# Your OpenAI API key (server-side only — never commit the real key).
OPENAI_API_KEY=sk-...

# Optional overrides:
# OPENAI_MODEL=gpt-4.1
# OPENAI_BASE_URL=https://api.openai.com/v1
